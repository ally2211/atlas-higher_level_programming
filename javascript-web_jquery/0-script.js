#!/usr/bin/node
// Use document.querySelector to select the <header> element
const headerElement = document.querySelector('header');

// Set the text color of the <header> element to red (#FF0000)
headerElement.style.color = '#FF0000';
